# boot.py runs once at power-on/reset, before main.py. Kept empty
# intentionally — all app logic (WiFi, HTTP server, print job handling)
# lives in main.py so there's one obvious place to read and edit.
